server:
  port: 8088

spring:
  application:
    name: chat-service
  config:
    import:
      - "optional:nacos:chat-service.yaml"
      - "optional:nacos:literature-common.yaml"
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
        file-extension: yaml
        username: nacos
        password: nacos
        namespace: literature-dev
        shared-configs:
          - data-id: literature-common.yaml
            refresh: true
      discovery:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
        username: nacos
        password: nacos
        namespace: literature-dev
